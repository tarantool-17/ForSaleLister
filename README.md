# ForSaleLister

ForSaleLister is a Codex-first assistant that turns product photos into researched, reviewable for-sale listings.

The initial version will run as a repository skill inside Codex. Marketplace connectors and a standalone user interface may be added after the workflow is validated.

## Status

Architecture and MVP planning.
